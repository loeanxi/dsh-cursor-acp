# Screenshots

Storefront images (PNG). After push, GitHub raw URLs can go into awesome-dsh-plugin `data/screenshots.json`.

1. `settings.png` — Settings → Cursor subagent (signed in, Test passed, model picker).
2. `job.png` — a chat where `cursor_agent` finished a small job and the parent sees the result.

Do not include email, tokens, or API keys in the shots.
